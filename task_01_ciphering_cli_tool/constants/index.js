const { OPEN_FILE_MODE } = require('./streams');
const { CLI_OPTIONS } = require('./cliParameters');
const { ALPHABET_CONFIG } = require('./alphabet');
const { CIPHER_MASK } = require('./cipher');
const { DIRNAME } = require('./paths');

module.exports = {
    OPEN_FILE_MODE,
    CLI_OPTIONS,
    ALPHABET_CONFIG,
    CIPHER_MASK,
    DIRNAME
};
